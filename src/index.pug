doctype html

html
  head
    title WGSL Shader Playground

    meta(charset="UTF-8")
    meta(name="viewport" content="width=device-width, initial-scale=1")

    link(rel="stylesheet" href="./index.css")

  body
    h1 WGSL Shader Playground
    div.container
      div.display
        canvas(id="canvas" width="600" height="600")
        div.no-gpu#no-gpu-error
          p #[strong WebGPU is not supported on this device!] This is a relavitely new API and likely will not work on your browser.
          p For a WebGL-based alternative, check out #[a(href!="https://www.shadertoy.com/") Shadertoy].
          p To see which browsers are currently supported, reference the #[a(href!="https://developer.mozilla.org/en-US/docs/Web/API/WebGPU_API#browser_compatibility") MDN WebGPU API compatibility document].
      div.editor
        div(id="editor")
        input(id="compile", type="button")

    script(src="./index.ts" type="module")
